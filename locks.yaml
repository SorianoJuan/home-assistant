  - name: Humidificador
    platform: mqtt
    state_topic: "invernadero/humidificador/lock/state"
    command_topic: "invernadero/humidificador/lock/set"
    payload_lock: "on"
    payload_unlock: "off"
    optimistic: false
    qos: 2
    retain: true
  - name: Estufa 1
    platform: mqtt
    state_topic: "invernadero/estufa1/lock/state"
    command_topic: "invernadero/estufa1/lock/set"
    payload_lock: "on"
    payload_unlock: "off"
    optimistic: false
    qos: 2
    retain: true
  - name: Ventilador 1
    platform: mqtt
    state_topic: "invernadero/ventilador1/lock/state"
    command_topic: "invernadero/ventilador1/lock/set"
    payload_lock: "on"
    payload_unlock: "off"
    optimistic: false
    qos: 2
    retain: true
  - name: Ventana 1
    platform: mqtt
    state_topic: "invernadero/ventana1/lock/state"
    command_topic: "invernadero/ventana1/lock/set"
    payload_lock: "on"
    payload_unlock: "off"
    optimistic: false
    qos: 2
    retain: true
  - name: Ventana 2
    platform: mqtt
    state_topic: "invernadero/ventana2/lock/state"
    command_topic: "invernadero/ventana2/lock/set"
    payload_lock: "on"
    payload_unlock: "off"
    optimistic: false
    qos: 2
    retain: true
  - name: Ventana 3
    platform: mqtt
    state_topic: "invernadero/ventana3/lock/state"
    command_topic: "invernadero/ventana3/lock/set"
    payload_lock: "on"
    payload_unlock: "off"
    optimistic: false
    qos: 2
    retain: true
  - name: Ventana 4
    platform: mqtt
    state_topic: "invernadero/ventana4/lock/state"
    command_topic: "invernadero/ventana4/lock/set"
    payload_lock: "on"
    payload_unlock: "off"
    optimistic: false
    qos: 2
    retain: true
  - name: Bomba 1
    platform: mqtt
    state_topic: "invernadero/bomba1/lock/state"
    command_topic: "invernadero/bomba1/lock/set"
    payload_lock: "on"
    payload_unlock: "off"
    optimistic: false
    qos: 2
    retain: true
  - name: Bomba 2
    platform: mqtt
    state_topic: "invernadero/bomba2/lock/state"
    command_topic: "invernadero/bomba2/lock/set"
    payload_lock: "on"
    payload_unlock: "off"
    optimistic: false
    qos: 2
    retain: true



#command_topic (Required): The MQTT topic to publish commands to change the lock state.
#name (Optional): The name of the lock. Default is ‘MQTT Lock’.
#state_topic (Optional): The MQTT topic subscribed to receive state updates.
#payload_lock (Optional): The payload that represents enabled/locked state. Default is “LOCK”.
#payload_unlock (Optional): The payload that represents disabled/unlocked state. Default is “UNLOCK”.
#optimistic (Optional): Flag that defines if lock works in optimistic mode. Default is true if no state_topic defined, else false.
#qos (Optional): The maximum QoS level of the state topic. Default is 0 and will also be used to publishing messages.
#retain (Optional): If the published message should have the retain flag on or not.
#value_template (Optional): Defines a template to extract a value from the payload.
